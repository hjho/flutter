void main() { /*
// ## 2.0 - Basic Data Types
  String name = "hjho";
  bool alive = true;
  int age = 28;
  double kg = 72.3;
  
  // int와 double의 부모.
  num x = 12;
  x = 1.1;
  
// ## 2.1 - Lists (collection if)
  // List<int> numbers = [1, 2, 3, 4];
  var numbers = [1, 2, 3, 4,];
  numbers.add(5);
  // collection if, collenction for
  var giveMeFive = true;
  var numbers2 = [
    1, 2, 3, 4,
    if(giveMeFive) 5,
  ];
  print(numbers2);
  
// ## 2.2 - String Interpolation
  var name = 'hjho';
  var age = 28;
  var greeting = 'Hello everone, my name is $name and I\'m ${age + 1}, nice to meet you!';
  print(greeting);
  
// ## 2.3 - Lists (collenction for)
  var oldFriends = ['heo', 'jun', 'ho'];
  var newFriends = [
    'name',
    'test',
    for(var friend in oldFriends) "❤ $friend",
  ];
  print(newFriends); // [name, test, ❤ heo, ❤ jun, ❤ ho]
  
// ## 2.4 - Maps
  // Map<String, Object>
  var player = {
    'name': 'hjho',
    'xp': 19.99,
    'superpower': false,
  };
  print(player); // {name: hjho, xp: 19.99, superpower: false}
  print(player.keys); // (name, xp, superpower)
  
// ## 2.5 - Sets
  // Set<int> 값이 유니크 하다.
  var numbers = {1, 2, 3, 4};
  numbers.add(1);
  numbers.add(1);
  numbers.add(1);
  print(numbers); // {1, 2, 3, 4}
  */
}