void main() { /*
// ## 1.0 - Hello World
  print('hello world!');
  
// ## 1.1 - The Var keyword 
  // 지역변수 내에서는 var 가 스타일가이드임.
  var name1 = 's';
  //name1 = 1; // 금지됨.
  String name2 = '허준';
  name2 = 'heojun';

// ## 1.2 - Dynamic Type
  // type is dynamic
  // 값이 어떤 타입인지 모른다.
  // var name;
  dynamic name;
  name = 'heojun';
  name = true;
  if(name is String) {
    name.isEmpty;
  }
  if(name is int) {
    name.isEven;
  }
  
// ## 1.3 - Nullable Variables
  // dart는 기본적으로 null을 사용하지 못함.
  String? hjho = 'heojun';
  hjho = null;
  hjho.length;
  if(hjho != null) {
    hjho.isNotEmpty;
  }
  // 축약형 코드.
  hjho?.isNotEmpty;
  
// ## 1.4 - Final Variables
  // final String name = 'hjho';
  final name = 'hjho';
  name = 'hjho2';
  
// ## 1.5 - Late Variables
  // late는 초기 데이터 없이 변수를 선언할 수 있게 해줌.
  late final String name;
  // do something, go to api
  // 초기화 전에는 사용할 수 없음.
  paring(name);
  name = 'hjho';
  // 초기화 이후 재설정 X
  name = 'heo';
  
// ## 1.6 - Constant Variables
  // const는 complie-time에 알고 있는 값이어야 함.
  const API_KEY = 'KEY121212';
  */
}